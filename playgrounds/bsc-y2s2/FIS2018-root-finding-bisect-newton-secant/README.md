# Root finding: bisection, Newton, secant
The animated iterates show how each method approaches a root of $f(x)$. Newton typically converges fastest near a simple root but can diverge from a bad starting guess; bisection always works inside a sign-changing bracket. Source: Villate Ch. 4 (`villate-vpython`).
